/* this is an example comment*/
import engine.parser.factory.slidingWindowFunctionFactory.SlidingTimeWindowFactory as stw


a :- b. //this rule is awsome
a.
b :- k at T, h in [t 5], l always in [# 10]. % this one is really long
f :- not r.

%* another comment
   over multiple lines
 *%
